#include "Tokeniser.hpp"

namespace Parser {

	using namespace std;

	vector<Token> Tokeniser::parse(const string& inProgram) {
		vector<Token> Tokens;
		Token currentToken;
			
		for (char curChr : inProgram) {
			switch (curChr) {

			}
		}

		return Tokens;
	}

}