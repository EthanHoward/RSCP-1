// Not needed import due to it being "Sys" which is rws standard
$incl("Sys");

void main(void)
{
    double foo = 1.3;
    Sys.out.println("Hey! User, heres an example of an equasion %d", 236 - 192);
}